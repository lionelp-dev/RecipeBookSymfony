{% extends 'base.html.twig' %}

{% block title %}Ajouter une recette{% endblock %}

{% block body %}
<div class="min-h-screen bg-slate-100 text-slate-950">
    <div class="mx-auto flex min-h-screen w-full max-w-3xl flex-col px-6 py-8">
        <div class="mb-6">
            <p class="text-sm text-slate-500">Recettes</p>
            <h1 class="text-2xl font-semibold">Ajouter une recette</h1>
        </div>

        <div class="rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
            {{ include('recipe/_form.html.twig', {button_label: 'Créer la recette'}) }}
        </div>
    </div>
</div>
{% endblock %}
